# Fix numpy/crewAI on NixOS: libstdc++.so.6 is not in default LD path
if [[ -d /nix/store ]]; then
    GCC_LIB=$(echo /nix/store/*-gcc-*-lib/lib 2>/dev/null | head -1)
    if [[ -d "$GCC_LIB" ]]; then
        export LD_LIBRARY_PATH="$GCC_LIB${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
    fi
fi
