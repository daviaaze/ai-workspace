Hey there! Happy to break this down. I've actually built pieces of what you're describing, just in a different industry, if you are interested lets have a call to see if I match what you expect! What time works best for you? Mornings or evenings should be fine since I live in a timezone GMT-3.

Basically I've built systems that connect people to the right thing at the right time, whether that's a hotel guest to optimized content, or a case manager to the right program for their client. Different domains, same logic. I'd genuinely enjoy applying it somewhere that matters.

 React & scalable apps
 I've worked on 5+ React projects from scratch to production. Configurable forms, dashboards that adapt to user roles, multi-tenant setups where different offices can't see each other's data, and apps supporting multiple languages. The patterns your platform needs — like case managers seeing one dashboard and probation officers seeing another. Permissions, role-based views, data isolation per team.

 Figma to production
 I use Figma extensively at my current job. Designers hand off high-fidelity mockups and I take them all the way to production. I also take care of underlying details: loading states so the UI doesn't jump around, error messages that actually tell the user what happened, multi-step forms that don't lose your progress, and making sure it works on mobile too.

 AI features
 I've wired OpenAI into real features people use:

 - A content optimization tool that takes hotel info and rewrites it for specific audiences. Not too different from matching a client profile to the right programs.
 - A PDF pipeline where you upload a document and AI pulls out the structured data and suggests changes. If a case manager uploads a client assessment and the system auto-extracts what programs they qualify for.
 - An email classifier that decides whether an incoming request can be handled automatically or needs a person to jump in. Your referral system has the same problem; some referrals are clear-cut and can auto-route, others are messy and need human judgment.

Backend side
 APIs, auth, database design, background jobs for the AI stuff, and logging that doesn't become useless noise. I've handled all of it on the Node.js side.
